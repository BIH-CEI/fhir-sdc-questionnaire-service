# HAPI FHIR Configuration for Site A (Collection Site)
# PROMIS v1.0 (older scoring algorithm)

hapi:
  fhir:
    # Server settings
    fhir_version: R4

    # Clinical Reasoning Module (enables CQL scoring)
    cr:
      enabled: true

    # Implementation Guides - Site A uses older versions to simulate version mismatch
    implementationguides:
      # SDC (Structured Data Capture) Implementation Guide
      sdc:
        name: hl7.fhir.uv.sdc
        version: 3.0.0
        installMode: STORE_AND_INSTALL
        reloadExisting: false
        fetchDependencies: false

      # MII PRO package
      mii_pros:
        name: de.medizininformatikinitiative.kerndatensatz.pros
        version: 2026.2.0
        installMode: STORE_AND_INSTALL
        packageUrl: https://packages.simplifier.net/de.medizininformatikinitiative.kerndatensatz.pros/2026.2.0
        reloadExisting: false
        fetchDependencies: false

    # Validation
    validation:
      enabled: true
      requests_enabled: true

    # Subscription (disabled for workshop)
    subscription:
      enabled: false

    # External terminology server
    # Points to workshop terminology-proxy
    remote_terminology_server_url: http://terminology-proxy:3000/fhir
